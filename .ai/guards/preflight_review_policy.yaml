# Preflight Review policy.
# Advisory-first by default. Enable gating explicitly if a repository wants
# status-based failures for selected Preflight Review states.
# When enabled, a valid Decision Evidence record and a fresh ready Preflight
# are required before the start and finish lifecycle paths can proceed.

version: 1
gateEnabled: false
blockedStatuses: []
