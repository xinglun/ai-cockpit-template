{
  "schemaVersion": 1,
  "execution": "Run npm install, npm run build, npm test, npm run lint, npm run format:check, and npm run lifecycle in this directory.",
  "externalProviderEvidence": {
    "status": "not_run",
    "reason": "The fixture has no external repository, provider credentials, identity, or enterprise control context."
  },
  "boundary": "Local executable fixture evidence is not proof of sandbox isolation, trusted identity, immutable audit, or compliance."
}
